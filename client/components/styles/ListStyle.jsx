import styled from 'styled-components';

const ListWrapper = styled.div`
  display: flex;
  height: 100%;
  width: 100%;
  background: black;
  /* align content
  rows
  overflow
  pagination */
`;

export default ListWrapper;